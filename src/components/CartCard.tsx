type Props = {
    img: string,
    name: string,
    price: number
}

export default function CartCard({ img, name, price }: Props) {
    return (
        <div className="h-16 w-full mx-10 rounded-sm bg-gray-300">
            hey
        </div>
    );
}
